- name: tmux - Set up tmux package manager
  ansible.builtin.git:
    repo: "git@github.com:tmux-plugins/tpm.git"
    dest: "{{ ansible_env.HOME }}/.tmux/plugins/tpm"
    force: no
  become: true
  become_user: "{{ target_user }}"
  tags: ['workstation-ubuntu','tmux', 'workstation-fedora']
